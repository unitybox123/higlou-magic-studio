import type { PublicMarketplaceListing } from "@/types/marketplace";

/** Empty fallback — only real Higlou → Don Baraton publishes appear. */
export const DEMO_MARKETPLACE_LISTINGS: PublicMarketplaceListing[] = [];
